# Environment Variables
# Copy this file to .env.local and fill in the values.

# API base URL — leave empty for local development (uses relative URLs).
# Set to the full production URL when deploying behind a CDN or custom domain.
# Example: https://foot-insights.vercel.app
NEXT_PUBLIC_API_BASE=
